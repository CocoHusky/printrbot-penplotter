# Release 1.0.10 — Multi-line Graves Layout

Graves handwriting now supports multi-line notes correctly.

- Explicit paragraphs are preserved.
- Long lines automatically wrap using the configured physical width and font
  size.
- Each generated line receives its own baseline and configured line spacing.
- The model's native stroke generation remains unchanged within each line.

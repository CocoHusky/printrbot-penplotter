# Release 1.0.8 — Stop Rendering

The Write workspace now provides a **Stop rendering** button while a preview
is being generated. It aborts the browser request, clears the incomplete
result, and returns the page to a state where the user can change settings or
render again.

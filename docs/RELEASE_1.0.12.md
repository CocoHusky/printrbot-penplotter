# Release 1.0.12 — Experimental Centerline Baselines

The experimental installed-font centerline converter now caches repeated glyph
conversions and documents its Cartesian baseline contract. Regression coverage
protects normal-letter baselines and descenders such as `y`, `p`, and `g`.
